Place the exported T800 qualifier joint policy here as `t800_qualifier_joint_policy.onnx`.
The SDK `rl_dance_example_runner` can load this file through ONNXRuntime when built with `/opt/onnxruntime`.
